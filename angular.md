# create a projecct:
ng new birthday-website
# to run:
ng serve
# generate components: 
ng generate component pages/greeting
ng generate component pages/gallery
ng generate component pages/thankyou

# In app.route.ts: 
we do add the components, such that it will redirect from there.  


# keyframes:
# What is @keyframes in CSS?

@keyframes is used to define an animation sequence.

It tells the browser:

How an element should change over time during an animation.

You define start state → intermediate state → end state.